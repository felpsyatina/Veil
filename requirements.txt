# Telegram-бот
aiogram>=3.15,<4.0

# Веб-API (вебхуки платежей, subscription-ссылки)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
slowapi>=0.1.9,<1.0

# База данных
sqlalchemy[asyncio]>=2.0.35,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# Настройки и валидация
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# Redis (FSM, троттлинг, кэш)
redis>=5.2,<6.0

# HTTP-клиент для 3x-ui API и платёжных провайдеров
httpx>=0.27,<1.0

# Планировщик фоновых задач (worker)
apscheduler>=3.10,<4.0

# QR-коды для конфигов
qrcode[pil]>=8.0,<9.0

# Шифрование секретов нод (Fernet)
cryptography>=43.0,<44.0

# Повторные попытки для сетевых вызовов (3x-ui / платёжный провайдер)
tenacity>=9.0,<10.0

python-dotenv>=1.0,<2.0
